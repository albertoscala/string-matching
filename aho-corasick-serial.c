#include <stdio.h>
#include <stdlib.h>

#include "aho-corasick.h"

void search(struct TrieNode* root, char *haystack, struct HashTable* table);

int main(void) {

    return 0;
}

void search(struct TrieNode* root, char *haystack, struct HashTable* table) {
    
}